# 统一登录（SSO）接入指南

> 面向：故事共创 / 深海基地重建 / 职业体验 三个模块的开发者
> 目的：说明如何把各自模块接入总平台的统一登录
> 参考实现：本文所有方案已在"自然聊天观察"模块（AI-talent scout 项目）中完整跑通

## 共享密钥（直接使用）

四个模块共用同一把 JWT 签名密钥，用于验证总平台签发的 `sso_token`：

```
PLATFORM_SSO_SECRET=platform-sso-demo-secret-do-not-use-in-production
```

> 这是当前团队内部演示阶段统一使用的密钥值，请存进各自项目的 `.env` 文件（变量名可自定，比如 `SSO_SECRET_KEY`），**不要提交到公开的 git 仓库**。这把密钥后续如果切换到正式部署环境，需要团队统一更换为更高强度的随机值，届时会另行通知。

---

## 你属于哪种情况？先看这张表

| 模块 | 你的入口文件 | 技术栈 | 该看哪部分 |
|---|---|---|---|
| 深海基地重建 | `public/deep-sea/index.html`（纯静态），通过 `src/pages/CampusDesign/index.tsx` 用 iframe 嵌入 | 纯前端 HTML/JS，无后端 | 场景一 |
| 职业体验 | 独立 FastAPI 后端 + `src/pages/CareerSim/index.tsx` 用 iframe 嵌入（端口需自行确认，8000 或 8005） | Python / FastAPI | 场景一 + Python 示例 |
| 故事共创 | `src/modules/StoryCreate/`（React组件，直接渲染，非iframe）+ `story-backend/`（已有自己的登录系统，`JWT_SECRET_KEY`） | React + Python / FastAPI | 场景二 + Python 示例 |

> 以上模块与负责人的对应关系请自行核实，本文只按架构分类，不涉及具体人员分工。

---

## 总平台已经做好的事（不用你们改）

在开始之前先明确一点：**总平台这边已经把"发送 token"这一半做完了**。四个模块的入口卡片链接，点击时都会自动携带 `?sso_token=xxx` 这个参数（定义在 `src/pages/Login/index.tsx`）。学生从总平台点进你的模块时，URL 里已经带着一个可以验证身份的 JWT，你要做的只是"接住它、认出它"。

```
学生在总平台登录
        │
        ▼
总平台签发 sso_token（JWT，有效期30分钟）
        │
        ▼
点击你的模块卡片 → 跳转到你的模块地址，自动带上 ?sso_token=xxx
        │
        ▼
【你需要做的部分从这里开始】
```

---

## 场景一：模块本来没有登录系统（深海基地重建 / 职业体验）

如果你的模块之前完全没有账号概念，接入反而更简单——不需要做"账号迁移"，只需要"认出这个人是谁"就行。

### 步骤

**1. 前端检测 URL 参数**

模块入口页面加载时，检测 URL 里有没有 `sso_token`：

```javascript
const params = new URLSearchParams(window.location.search);
const ssoToken = params.get('sso_token');

if (ssoToken) {
  // 有 token，去验证
} else {
  // 没有 token，不要显示功能页面，
  // 显示"请从总平台进入"的引导提示
}
```

**2a. 后端验证（Node.js / Express 示例）**

```bash
npm install jsonwebtoken
```

```javascript
const jwt = require('jsonwebtoken');

app.post('/api/sso-verify', (req, res) => {
  const { sso_token } = req.body;

  if (!sso_token) {
    return res.status(400).json({ error: '缺少 sso_token' });
  }

  let payload;
  try {
    payload = jwt.verify(sso_token, process.env.SSO_SECRET_KEY, {
      algorithms: ['HS256']
    });
  } catch (err) {
    return res.status(401).json({ error: 'token 无效或已过期' });
  }

  const { platformUid, username } = payload;
  // platformUid 例如 "S2024001"，username 例如 "小明"

  req.session.platformUid = platformUid;
  req.session.username = username;

  res.json({ ok: true, platformUid, username });
});
```

**2b. 后端验证（Python / FastAPI 示例，适用于职业体验）**

```bash
pip install pyjwt
```

```python
import jwt
import os
from fastapi import FastAPI, HTTPException, Body

app = FastAPI()
SSO_SECRET_KEY = os.environ.get("SSO_SECRET_KEY")

@app.post("/api/sso-verify")
def sso_verify(sso_token: str = Body(..., embed=True)):
    if not sso_token:
        raise HTTPException(status_code=400, detail="缺少 sso_token")

    try:
        payload = jwt.decode(
            sso_token,
            SSO_SECRET_KEY,
            algorithms=["HS256"]
        )
    except jwt.ExpiredSignatureError:
        raise HTTPException(status_code=401, detail="token 已过期")
    except jwt.InvalidTokenError:
        raise HTTPException(status_code=401, detail="token 无效")

    platform_uid = payload.get("platformUid")
    username = payload.get("username")
    # platform_uid 例如 "S2024001"，username 例如 "小明"

    # 把 platform_uid 存进 session，或者作为这次访问的身份标识
    # 后续这个学生产生的数据，都关联到这个 platform_uid

    return {"ok": True, "platformUid": platform_uid, "username": username}
```

**3. 没有 token 时的引导页**

不要让没带 token 的访问者直接看到功能页面：

```html
<div>
  <p>请从"AI 伯乐"总平台进入本模块</p>
  <a href="http://localhost:5173/login">前往总平台</a>
</div>
```

（正式部署后把地址换成真实域名，建议做成环境变量方便切换）

### 就这么多

因为没有旧账号要处理，场景一只需要"验证 token → 记住这个人是谁 → 关联数据"三步。

---

## 场景二：模块已有自己的登录系统（故事共创）

这个场景多了一步——你已经有自己的账号体系（`story-backend/`，已有 `JWT_SECRET_KEY`），需要让"总平台的身份"和"你自己的账号"对应起来，而不是推翻重做。

### 核心思路

不要用 `platformUid` 替换你现有的账号主键，而是给你的用户表**新增一列**，专门存 `platformUid`，作为两边账号的关联字段。

```
你现在的账号表：
user_id | username | password_hash | ...

改成：
user_id | username | password_hash | platform_uid | ...
                                        ↑ 新增这一列，其他都不动
```

### 步骤（Python / FastAPI 示例）

**1. 使用共享密钥**（文档开头的 `PLATFORM_SSO_SECRET`，注意这跟你自己现有的 `JWT_SECRET_KEY` 是两把完全不同的密钥，不要混用）

**2. 加一个 SSO 登录接口**，跟你现有的账号密码登录接口是平行的两条路：

```python
import jwt
import os
import bcrypt
from fastapi import FastAPI, HTTPException, Body

app = FastAPI()
SSO_SECRET_KEY = os.environ.get("PLATFORM_SSO_SECRET")

@app.post("/api/auth/sso-login")
def sso_login(sso_token: str = Body(..., embed=True)):
    try:
        payload = jwt.decode(sso_token, SSO_SECRET_KEY, algorithms=["HS256"])
    except jwt.ExpiredSignatureError:
        raise HTTPException(status_code=401, detail="token 已过期")
    except jwt.InvalidTokenError:
        raise HTTPException(status_code=401, detail="token 无效")

    platform_uid = payload.get("platformUid")
    username = payload.get("username")

    # 按 platform_uid 去你自己的用户表里查
    user = find_user_by_platform_uid(platform_uid)

    if not user:
        # 第一次通过 SSO 进来，自动建一个账号
        # 密码设成随机值（这个账号不需要靠密码登录）
        random_password = bcrypt.gensalt().decode()
        user = create_user(
            username=username or platform_uid,
            platform_uid=platform_uid,
            password_hash=bcrypt.hashpw(random_password.encode(), bcrypt.gensalt())
        )

    # 复用你现有的"登录成功后要做的事"，
    # 比如生成你自己的 JWT_SECRET_KEY 签发的 token
    your_own_token = generate_your_token(user)

    return {"ok": True, "token": your_own_token, "user": user}
```

**3. 前端逻辑：优先走 SSO，没有 token 才用原来的登录方式**

```javascript
// 页面加载时
const ssoToken = new URLSearchParams(location.search).get('sso_token');

if (ssoToken) {
  const res = await fetch('/api/auth/sso-login', {
    method: 'POST',
    body: JSON.stringify({ sso_token: ssoToken })
  });
  const data = await res.json();
  localStorage.setItem('auth_token', data.token);
  // 登录完成，正常进入功能页面
} else {
  const existing = localStorage.getItem('auth_token');
  if (!existing) {
    // 都没有，显示"请从总平台进入"引导页
  }
}
```

**4. 老用户怎么办**

如果现在已经有一批用旧方式（账号密码）注册的用户，这批人**不受影响**——`platform_uid` 这一列对他们来说是空的（`null`），照样能用原来的方式登录。只有"新的、通过总平台进来的学生"才会用到这一套 SSO 逻辑，两条路并存，互不干扰。

---

## 通用的坑（我们已经踩过，你们可以直接避开）

### 坑一：密钥必须完全一致

`SSO_SECRET_KEY`（或 `PLATFORM_SSO_SECRET`，变量名可以不同，但**值必须一模一样**）要和总平台 `platform-auth` 那边用的密钥完全一致，包括大小写、空格都不能有差异。改了 `.env` 之后要重启服务才生效。

### 坑二：不要用命令行测试中文内容

如果要测试涉及中文用户名的功能，**一定要用浏览器网页表单操作**，不要在 Windows 命令行/PowerShell 里直接输入中文去调接口测试——Windows 终端默认编码是 GBK，会导致中文变成乱码（比如"小明"变成 `С??` 这种），排查起来容易走弯路，我们已经踩过好几次。

### 坑三：sso_token 只有 30 分钟有效期，页面刷新可能会掉

`sso_token` 是短期的，只在"跳转进门"那一下用。如果你的模块内部有路由跳转、或者用户刷新页面，URL 里的 `sso_token` 参数很容易丢失。建议：

- 拿到 token 验证成功后，**把你自己签发的登录状态（session/自己的token）存进 Cookie 或 localStorage**，不要依赖 URL 参数长期存在
- 模块内部页面之间跳转时，不要还指望 URL 里一直带着 `sso_token`

### 坑四：不要让"没有 token"的直接访问，显示完整的登录表单

如果你的模块有独立的账号密码登录表单，**不要让它成为"无 token 时的默认显示内容"**。正确做法：无 token 时先显示"请从总平台进入"的引导页，把账号密码登录表单收起来，只作为你自己开发调试时的备用入口（比如放在一个不会被首页链接到的独立地址）。这样能防止别人绕过总平台、直接对你的模块发起登录尝试。

---

## 遇到问题怎么办

- 密钥或整体方案相关的问题：在团队群里同步即可
- 实现细节或接入过程中的具体报错：可以参考"自然聊天观察"模块（AI-talent scout 项目）里 `server.js` 的 `/api/auth/sso-login` 接口和 `public/home.html` 的对应逻辑，那边是完整跑通过的参考实现，Node.js 写法可以照抄，Python 需要按本文的 PyJWT 示例改写
